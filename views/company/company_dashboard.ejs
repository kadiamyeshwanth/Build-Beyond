<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Build & Beyond - Construction Management System</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
    <style>
        :root {
            --primary-green: #4D7A70;
            --secondary-green: #6A9E8E;
            --light-green: #A3C5BB;
            --dark-grey: #363636;
            --medium-grey: #5A5A5A;
            --light-grey: #E0E0E0;
            --white: #FFFFFF;
            --black: #000000;
        }
        .progress-bar {
            width: calc(var(--progress) * 1%);
        }
        a:-webkit-any-link {
            cursor: pointer;
            text-decoration: none;
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: var(--white);
            color: var(--black);
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .main-container {
            flex: 1;
            padding: 2rem;
            background-color: var(--light-grey);
        }

        .page-title {
            margin-bottom: 1.5rem;
            color: var(--primary-green);
            font-size: 1.8rem;
        }

        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .stat-card {
            background-color: var(--white);
            border-radius: 8px;
            padding: 1.5rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .stat-card h3 {
            font-size: 1rem;
            color: var(--medium-grey);
            margin-bottom: 0.5rem;
        }

        .stat-card .stat-value {
            font-size: 1.8rem;
            font-weight: bold;
            color: var(--primary-green);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1rem;
            margin-top: 2rem;
        }

        .section-title {
            color: var(--medium-grey);
            font-size: 1.2rem;
        }

        .view-all {
            color: var(--primary-green);
            cursor: pointer;
            font-weight: 500;
        }

        .cards-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
        }

        .bid-card {
            background-color: var(--white);
            border-radius: 8px;
            padding: 1.5rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            border-top: 4px solid var(--secondary-green);
        }

        .bid-card h3 {
            margin-bottom: 0.8rem;
            color: var(--dark-grey);
        }

        .bid-info {
            margin-bottom: 1rem;
        }

        .bid-info p {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
            font-size: 0.9rem;
        }

        .bid-info span:first-child {
            color: var(--medium-grey);
        }

        .bid-info span:last-child {
            font-weight: 500;
        }

        .bid-actions {
            display: flex;
            gap: 1rem;
        }

        .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: 500;
            transition: background-color 0.3s;
        }

        .btn-primary {
            background-color: var(--primary-green);
            color: var(--white);
        }

        .btn-primary:hover {
            background-color: var(--secondary-green);
        }

        .btn-secondary {
            background-color: var(--light-grey);
            color: var(--medium-grey);
        }

        .btn-secondary:hover {
            background-color: #D0D0D0;
        }

        .timeline-container {
            background-color: var(--white);
            border-radius: 8px;
            padding: 1.5rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            margin-top: 2rem;
        }

        .timeline-project {
            display: flex;
            margin-bottom: 1.5rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid var(--light-grey);
        }

        .timeline-project:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }

        .project-info {
            flex: 1;
        }

        .project-info h3 {
            margin-bottom: 0.5rem;
            color: var(--dark-grey);
        }

        .project-details {
            display: flex;
            margin-bottom: 1rem;
            gap: 2rem;
        }

        .project-details p {
            font-size: 0.9rem;
            color: var(--medium-grey);
        }

        .project-details span {
            font-weight: 500;
            color: var(--dark-grey);
        }

        .progress-container {
            background-color: var(--light-grey);
            height: 8px;
            border-radius: 4px;
            margin-bottom: 0.5rem;
        }

        .progress-bar {
            height: 100%;
            border-radius: 4px;
            background-color: var(--secondary-green);
        }

        .progress-info {
            display: flex;
            justify-content: space-between;
            font-size: 0.8rem;
            color: var(--medium-grey);
        }

        .project-status {
            align-self: flex-start;
            background-color: #E5F4EF;
            color: var(--primary-green);
            padding: 0.3rem 0.8rem;
            border-radius: 4px;
            font-size: 0.8rem;
            font-weight: 500;
        }

        .status-delayed {
            background-color: #FFF3E0;
            color: #FF9800;
        }
    </style>
</head>
<body>
    <%- include('navbar_company') %>
    <div class="main-container">
        <h1 class="page-title">Dashboard</h1>

        <div class="stats-container">
            <div class="stat-card">
                <h3>Active Projects</h3>
                <div class="stat-value"><%= activeProjects %></div>
            </div>
            <div class="stat-card">
                <h3>Total Projects Completed</h3>
                <div class="stat-value"><%= completedProjects %></div>
            </div>
            <div class="stat-card">
                <h3>This Month Revenue</h3>
                <div class="stat-value">₹<%= revenue.toLocaleString('en-IN') %></div>
            </div>
        </div>

        <div class="section-header">
            <h2 class="section-title">New Bids</h2>
            <div class="view-all"><a href="/companybids.html">View All</a></div>
        </div>

        <div class="cards-container">
            <% bids.forEach(bid => { %>
                <div class="bid-card">
                    <h3><%= bid.projectName %></h3>
                    <div class="bid-info">
                        <p>
                            <span>Client:</span>
                            <span><%= bid.customerName %></span>
                        </p>
                        <p>
                            <span>Location:</span>
                            <span><%= bid.projectLocation %></span>
                        </p>
                        <p>
                            <span>Budget:</span>
                            <span>₹<%= bid.estimatedBudget ? bid.estimatedBudget.toLocaleString('en-IN') : 'TBD' %></span>
                        </p>
                        <p>
                            <span>Timeline:</span>
                            <span><%= bid.projectTimeline ? bid.projectTimeline + ' months' : 'TBD' %></span>
                        </p>
                        <p>
                            <span>Due Date:</span>
                            <span><%= new Date(bid.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></span>
                        </p>
                    </div>
                    <div class="bid-actions">
                        <button class="btn btn-primary">Review Details</button>
                        <button class="btn btn-secondary">Decline</button>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="section-header">
            <h2 class="section-title">New Projects</h2>
            <div class="view-all"><a href="/project_requests.html">View All</div></a>
        </div>

        <div class="cards-container">
            <% projects.filter(project => project.status === 'pending').forEach(project => { %>
                <div class="bid-card">
                    <h3><%= project.projectName %></h3>
                    <div class="bid-info">
                        <p>
                            <span>Client:</span>
                            <span><%= project.customerName %></span>
                        </p>
                        <p>
                            <span>Location:</span>
                            <span><%= project.projectAddress %></span>
                        </p>
                        <p>
                            <span>Budget:</span>
                            <span>₹<%= project.estimatedBudget ? project.estimatedBudget.toLocaleString('en-IN') : 'TBD' %></span>
                        </p>
                        <p>
                            <span>Timeline:</span>
                            <span><%= project.projectTimeline ? project.projectTimeline + ' months' : 'TBD' %></span>
                        </p>
                        <p>
                            <span>Due Date:</span>
                            <span><%= new Date(project.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></span>
                        </p>
                    </div>
                    <div class="bid-actions">
                        <button class="btn btn-primary">Review Details</button>
                        <button class="btn btn-secondary">Decline</button>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="section-header" style="margin-top: 2rem;">
            <h2 class="section-title">Project Timeline</h2>
            <div class="view-all"><a href="/companyongoing_projects.html">View All Projects</a></div>
        </div>

        <div class="timeline-container">
            <% projects.filter(project => project.status === 'accepted').forEach(project => { %>
                <div class="timeline-project">
                    <div class="project-info">
                        <h3><%= project.projectName %></h3>
                        <div class="project-details">
                            <p>Client: <span><%= project.customerName %></span></p>
                            <p>Start: <span><%= new Date(project.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></span></p>
                            <p>End: <span><%= project.projectTimeline ? new Date(new Date(project.createdAt).setMonth(new Date(project.createdAt).getMonth() + project.projectTimeline)).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) : 'TBD' %></span></p>
                        </div>
                        <div class="progress-container">
                            <div class="progress-bar" style="width: <%= calculateProgress(project.createdAt, project.projectTimeline) %>%;"></div>
                        </div>
                        <div class="progress-info">
                            <span>Progress: <%= calculateProgress(project.createdAt, project.projectTimeline) %>%</span>
                            <span><%= calculateDaysRemaining(project.createdAt, project.projectTimeline) %> days remaining</span>
                        </div>
                    </div>
                    <div class="project-status <%= calculateProgress(project.createdAt, project.projectTimeline) < 50 ? 'status-delayed' : '' %>">
                        <%= calculateProgress(project.createdAt, project.projectTimeline) < 50 ? 'Delayed' : 'On Track' %>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>

    <script>
        // Note: This script is minimal as per request to avoid script-based dynamism
        document.addEventListener('DOMContentLoaded', function() {
            const userProfile = document.querySelector('.user-profile');
            // Add any client-side interactivity if needed
        });
        document.querySelectorAll('.progress-bar').forEach(bar => {
            bar.style.setProperty('--progress', bar.dataset.progress);
        });
    </script>
</body>
</html>